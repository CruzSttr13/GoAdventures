// we define the package name as main, which means that this is the entry point of our application.
package main

//importing the fmt package, which contains functions for printing text to the console.
import "fmt"

//The main function is the entry point of our application.
func main() {
	fmt.Println("It's better to use the run command to skip a step.")

}

//commands for console
//"go run main.go" to run the program in the console
//"go build main.go" to build the program in the console
//"./main" to run the program in the console after building it
